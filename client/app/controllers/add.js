angular.module('farmer.add', ['farmer.services'])

.controller('AddController', function($scope, Add) {
  // here lies the function that will take the form submission from the user and send it off as a POST request to add a new market to the DB
})
